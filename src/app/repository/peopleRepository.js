class PeopleRepository {
  constructor(table) {
    this.table = table;
  }

  findById(id) {
    //this.table.findById(id);
    return null
  }

  savePeople() {}
}

module.exports = PeopleRepository;
